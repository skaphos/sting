# PR fixtures

All provider traffic uses the local GET-only fixture in `ghclient/pr_test.go`.
Times are fixed relative to 2026-08-25T13:00:00Z. Scenario-specific fixtures live
beside their tests or in subdirectories here; no provider credentials are used.
