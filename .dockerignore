# The image is built from binaries GoReleaser has already compiled (see the
# dockers_v2 block in .goreleaser.yaml), not from source. Keep the build context
# to what the Dockerfile actually copies.

.git/
.github/
.claude/
.specify/

dist/
specs/
docs/
graphify-out/
tools/

*.log
*.tmp
*.swp
.env*
.DS_Store
Thumbs.db

coverage.out
coverage.txt
